# '어제랑 비교해서' 축 확장 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 장중 대결판에 **강도(거래량·진폭) · 주도권(지수·섹터) · 누가(기관 세부)** 세 축을 더하고, 마감 후·밤·장 전·주말 시간대별로 보여줄 카드를 나눈다.

**Architecture:** 순수 계산 모듈을 `api/_vs-axes.mjs` 하나에 모으고, 수집은 기존 `_vs-prices.minuteBars` / `_vs-flow.flowAt`를 재사용한다. `_vs-intraday.buildIntradayVs`가 그 결과를 응답에 `axes` 키로 덧붙이고, 클라이언트 `vs-yesterday.js`가 카드를 그린다. 마감 후 슬롯은 `_vs-close.mjs`가 15:40 수급 행과 정규장 1분봉으로 따로 만든다.

**Tech Stack:** Vanilla ES modules(Vercel Functions) · `node:test` · 브라우저 JS(IIFE, 빌드 없음) · 프로덕션 CSS(`vs-yesterday.css`)

**Spec:** [docs/superpowers/specs/2026-09-16-vs-axes-design.md](../specs/2026-09-16-vs-axes-design.md)

## Global Constraints

- **라우트를 늘리지 않는다.** Vercel Hobby 12개 한도(§46). 새 파일은 `api/_` 접두로 만든다 — 접두가 없으면 라우트로 배포돼 한도를 넘긴다.
- **`api/`에 테스트를 넣을 때도 `_` 접두를 쓴다**(`api/_vs-axes.test.mjs`). `api/_route-budget.test.mjs`가 이를 검사한다.
- **기준가는 직전 거래일 15:30 1분봉**(`_vs-prices.prevClose`). 일봉 `day` 엔드포인트를 종가로 쓰지 않는다(§48).
- **마감 후 수급은 15:31~15:40 행**(§51). 16:00 이후 실시간·일봉은 애프터장 값이라 쓰지 않는다.
- **날짜 고정 조회**(§45). 어제 값은 그 날짜 행만 쓰고, 없으면 `null`. 최신 행으로 폴백 금지.
- **차이는 미반올림 값으로 계산한다.** `round2(t) - round2(y)`가 아니라 `round2(t - y)`.
- **라벨은 "거래량"이다.** "거래대금"이라 쓰지 않는다 — 지수 1분봉에는 거래대금이 없다.
- **대결판은 오늘 값을 다시 적지 않는다**(설계 §4). 근거 줄은 "어제 이 시각엔 −0.78%였어요. 지금은 +1.81%p 더 높아요" 형태다. 오늘 절대값은 LIVE 지수바가 단독으로 담당한다 — 둘 다 적으면 장중엔 시차(약 2분)로, 마감 후엔 애프터장 포함 여부로 같은 지표가 다른 숫자로 뜬다.
- **판정 임계**: 등락률 `TH.pctPoint = 0.3`(%p) · 거래량 `TH.volPct = 10`(%) · 진폭 `TH.pctPoint` · 수급은 부호 전환 여부.
- **기존 `vs-` CSS 클래스를 수정하지 않는다.** 새 스타일은 `vsx-` 접두로만 추가한다.
- 테스트 실행: `node --test api/*.test.mjs` / `node --test web/assets/*.test.mjs` (CI `.github/workflows/ci.yml:60-61`과 동일)
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

---

## File Structure

| 파일 | 책임 |
| --- | --- |
| `api/_vs-axes.mjs` (신규) | 세 축의 **순수 계산**. 네트워크 없음. 봉 배열·수급 행을 받아 축 객체를 낸다 |
| `api/_vs-axes.test.mjs` (신규) | 위 모듈 단위 테스트 + 9/16 실측 리플레이 |
| `api/_vs-sectors.mjs` (신규) | 섹터 대표 3종목 목록과 **평균·순위 계산**(순수). 종목 코드는 `scripts/config/stock_universe.json`에서 읽지 않고 상수로 둔다(서버리스에서 파일 접근을 줄인다) |
| `api/_vs-sectors.test.mjs` (신규) | 평균·순위·결측 처리 |
| `api/_vs-intraday.mjs` (수정) | 축 수집을 붙이고 응답에 `axes` 추가 |
| `api/_vs-close.mjs` (신규) | 마감 후 슬롯 — 정규장 확정 봉 + 15:40 수급 행 |
| `api/_vs-close.test.mjs` (신규) | 15:40 행 선택·시간대 게이트 |
| `api/intraday.mjs` (수정) | `vs=close` 분기 추가(라우트 증가 없음) |
| `web/assets/vs-yesterday.js` (수정) | 카드 렌더 + 시간대별 표시 규칙 |
| `web/assets/vs-yesterday.css` (수정) | `vsx-` 스타일 추가 |
| `web/assets/vs-yesterday.test.mjs` (수정) | 렌더·시간대 게이트 테스트 |

---

## Task 1: 강도 축 계산 (거래량·진폭)

**Files:**
- Create: `api/_vs-axes.mjs`
- Test: `api/_vs-axes.test.mjs`

**Interfaces:**
- Consumes: `_vs-core.mjs`의 `round2`, `judge`, `TH`
- Produces:
  - `TH_VOL = 10` (거래량 "비슷해요" 임계, %)
  - `heatAxis(barsT, barsY, baseT, baseY, at)` → `{ vol:{t,y,diff,judge}, amp:{t,y,diff,judge} }`
    - `barsT`/`barsY`: `[{t:'0900', v:Number, h:Number, l:Number, vol:Number}]` (시각 오름차순)
    - `at`: `'1330'` 형태. 이 시각 이하 봉만 쓴다
    - 값이 없으면 해당 하위 객체의 `t`/`y`/`diff`/`judge`는 `null`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// api/_vs-axes.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { heatAxis, TH_VOL } from './_vs-axes.mjs';

const bars = (rows) => rows.map(([t, v, h, l, vol]) => ({ t, v, h, l, vol }));

test('거래량은 at 이하 봉만 누적하고 차이를 %로 낸다', () => {
  const T = bars([['0900', 100, 101, 99, 10], ['0901', 100, 100, 100, 5], ['1400', 100, 100, 100, 99]]);
  const Y = bars([['0900', 100, 100, 100, 20], ['0901', 100, 100, 100, 5]]);
  const r = heatAxis(T, Y, 100, 100, '0901');
  assert.equal(r.vol.t, 15);
  assert.equal(r.vol.y, 25);
  assert.equal(r.vol.diff, -40);        // (15/25 - 1) * 100
  assert.equal(r.vol.judge, 'weak');    // |−40| > 10
});

test('진폭은 (고−저)/기준가 이고 임계 안이면 same', () => {
  const T = bars([['0900', 100, 102, 99, 1]]);
  const Y = bars([['0900', 100, 102, 99, 1]]);
  const r = heatAxis(T, Y, 100, 100, '0900');
  assert.equal(r.amp.t, 3);
  assert.equal(r.amp.diff, 0);
  assert.equal(r.amp.judge, 'same');
});

test('어제 봉이 없으면 비운다 — 오늘 값으로 대신하지 않는다', () => {
  const T = bars([['0900', 100, 101, 99, 10]]);
  const r = heatAxis(T, [], 100, null, '0900');
  assert.equal(r.vol.t, 10);
  assert.equal(r.vol.y, null);
  assert.equal(r.vol.diff, null);
  assert.equal(r.vol.judge, null);
  assert.equal(r.amp.y, null);
});

test('거래량 임계는 10%', () => { assert.equal(TH_VOL, 10); });
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test api/_vs-axes.test.mjs`
Expected: FAIL — `Cannot find module './_vs-axes.mjs'`

- [ ] **Step 3: 최소 구현**

```js
// api/_vs-axes.mjs
// '어제랑 비교해서' 세 축(강도·주도권·누가)의 순수 계산 — 네트워크 없음
import { round2, judge, TH } from './_vs-core.mjs';

export const TH_VOL = 10;   // 거래량 "비슷해요" 임계(%)

const upto = (bars, at) => (bars || []).filter((b) => b.t <= at);

function sumVol(bars) {
  if (!bars.length) return null;
  return bars.reduce((s, b) => s + (Number(b.vol) || 0), 0);
}

function amplitude(bars, base) {
  if (!bars.length || !(base > 0)) return null;
  const hs = bars.map((b) => b.h).filter((n) => typeof n === 'number');
  const ls = bars.map((b) => b.l).filter((n) => typeof n === 'number');
  if (!hs.length || !ls.length) return null;
  return round2((Math.max(...hs) - Math.min(...ls)) / base * 100);
}

// 두 값을 받아 {t,y,diff,judge}. 한쪽이라도 없으면 diff·judge는 null(§45 — 오늘 값으로 메우지 않는다)
function pair(t, y, diff, th) {
  const d = t != null && y != null ? diff : null;
  return { t, y, diff: d, judge: d == null ? null : judge(d, th) };
}

export function heatAxis(barsT, barsY, baseT, baseY, at) {
  const T = upto(barsT, at), Y = upto(barsY, at);
  const vt = sumVol(T), vy = sumVol(Y);
  const at_ = amplitude(T, baseT), ay = amplitude(Y, baseY);
  return {
    vol: pair(vt, vy, vt != null && vy > 0 ? round2((vt / vy - 1) * 100) : null, TH_VOL),
    amp: pair(at_, ay, at_ != null && ay != null ? round2(at_ - ay) : null, TH.pctPoint),
  };
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test api/_vs-axes.test.mjs`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add api/_vs-axes.mjs api/_vs-axes.test.mjs
git commit -m "feat: 대결판 강도 축 — 누적 거래량·일중 진폭 계산

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: 주도권 축 — 섹터 대표 3종목 평균과 순위

**Files:**
- Create: `api/_vs-sectors.mjs`
- Test: `api/_vs-sectors.test.mjs`

**Interfaces:**
- Consumes: `_vs-core.mjs`의 `round2`, `pct`
- Produces:
  - `SECTOR_REPS` — `[{key, label, codes:[3], names:[3]}]` 8개
  - `sectorRows(byCode)` → `[{key,label,names,t,y,diff,rank,prevRank,move,n}]` **오늘 등락률 내림차순**
    - `byCode`: `{ '005930': {t:Number|null, y:Number|null}, ... }`
    - `t`/`y`는 각 섹터에서 **값이 있는 종목만** 평균. 오늘 값이 하나도 없으면 그 섹터는 결과에서 빠진다
    - `move = prevRank - rank` (양수면 순위 상승)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// api/_vs-sectors.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { SECTOR_REPS, sectorRows } from './_vs-sectors.mjs';

test('8섹터 × 대표 3종목', () => {
  assert.equal(SECTOR_REPS.length, 8);
  for (const s of SECTOR_REPS) {
    assert.equal(s.codes.length, 3, s.key);
    assert.equal(s.names.length, 3, s.key);
  }
});

test('오늘 등락률 내림차순 정렬과 순위 이동', () => {
  const by = {};
  // 반도체는 오늘 1위·어제 2위, 금융은 오늘 2위·어제 1위가 되게 채운다
  const semi = SECTOR_REPS.find((s) => s.key === 'semicon');
  const fin = SECTOR_REPS.find((s) => s.key === 'finance');
  semi.codes.forEach((c) => { by[c] = { t: 2, y: 0 }; });
  fin.codes.forEach((c) => { by[c] = { t: 1, y: 1 }; });
  const rows = sectorRows(by);
  assert.equal(rows[0].key, 'semicon');
  assert.equal(rows[0].rank, 1);
  assert.equal(rows[0].prevRank, 2);
  assert.equal(rows[0].move, 1);
  assert.equal(rows[1].key, 'finance');
  assert.equal(rows[1].move, -1);
});

test('일부 종목이 없으면 있는 것만 평균 내고 n을 남긴다', () => {
  const semi = SECTOR_REPS.find((s) => s.key === 'semicon');
  const by = { [semi.codes[0]]: { t: 3, y: 1 }, [semi.codes[1]]: { t: 1, y: 1 } };
  const row = sectorRows(by).find((r) => r.key === 'semicon');
  assert.equal(row.t, 2);
  assert.equal(row.n, 2);
});

test('오늘 값이 하나도 없는 섹터는 빠진다', () => {
  const semi = SECTOR_REPS.find((s) => s.key === 'semicon');
  const by = {}; semi.codes.forEach((c) => { by[c] = { t: null, y: 1 }; });
  assert.equal(sectorRows(by).find((r) => r.key === 'semicon'), undefined);
});

test('차이는 반올림 전 값으로 계산한다', () => {
  const semi = SECTOR_REPS.find((s) => s.key === 'semicon');
  const by = {}; semi.codes.forEach((c) => { by[c] = { t: 1.035, y: -0.775 }; });
  assert.equal(sectorRows(by).find((r) => r.key === 'semicon').diff, 1.81);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test api/_vs-sectors.test.mjs`
Expected: FAIL — `Cannot find module './_vs-sectors.mjs'`

- [ ] **Step 3: 최소 구현**

```js
// api/_vs-sectors.mjs
// 섹터 대표 3종목 평균·순위 — 순수 계산. 종목은 stock_universe.json 각 섹터의 앞 3개와 같다.
// "섹터 평균"이 아니라 "대표 3종목 평균"이다(설계 §5 — 46종목 평균과 순위가 갈린다).
import { round2 } from './_vs-core.mjs';

export const SECTOR_REPS = [
  { key: 'semicon', label: '반도체', codes: ['005930', '000660', '042700'], names: ['삼성전자', 'SK하이닉스', '한미반도체'] },
  { key: 'power', label: '전력기기', codes: ['267260', '010120', '298040'], names: ['HD현대일렉트릭', 'LS일렉트릭', '효성중공업'] },
  { key: 'defense', label: '방산', codes: ['012450', '079550', '064350'], names: ['한화에어로스페이스', 'LIG넥스원', '현대로템'] },
  { key: 'ship', label: '조선', codes: ['329180', '042660', '010140'], names: ['HD현대중공업', '한화오션', '삼성중공업'] },
  { key: 'battery', label: '2차전지', codes: ['373220', '247540', '006400'], names: ['LG에너지솔루션', '에코프로비엠', '삼성SDI'] },
  { key: 'auto', label: '자동차', codes: ['005380', '000270', '012330'], names: ['현대차', '기아', '현대모비스'] },
  { key: 'bio', label: '바이오', codes: ['207940', '068270', '000100'], names: ['삼성바이오로직스', '셀트리온', '유한양행'] },
  { key: 'finance', label: '금융', codes: ['032830', '105560', '055550'], names: ['삼성생명', 'KB금융', '신한지주'] },
];

const mean = (a) => (a.length ? a.reduce((s, n) => s + n, 0) / a.length : null);

export function sectorRows(byCode) {
  const rows = [];
  for (const s of SECTOR_REPS) {
    const ts = s.codes.map((c) => byCode[c]?.t).filter((n) => typeof n === 'number');
    const ys = s.codes.map((c) => byCode[c]?.y).filter((n) => typeof n === 'number');
    if (!ts.length) continue;                 // 오늘 값이 없으면 그 섹터는 내보내지 않는다
    const t = mean(ts), y = ys.length ? mean(ys) : null;
    rows.push({
      key: s.key, label: s.label, names: s.names, n: ts.length,
      t: round2(t), y: y == null ? null : round2(y),
      diff: y == null ? null : round2(t - y),  // 반올림 전 값으로 뺀다
      _rawY: y,
    });
  }
  const byT = [...rows].sort((a, b) => b.t - a.t);
  const withY = rows.filter((r) => r._rawY != null).sort((a, b) => b._rawY - a._rawY);
  byT.forEach((r, i) => {
    r.rank = i + 1;
    const j = withY.findIndex((x) => x.key === r.key);
    r.prevRank = j < 0 ? null : j + 1;
    r.move = r.prevRank == null ? null : r.prevRank - r.rank;
    delete r._rawY;
  });
  return byT;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test api/_vs-sectors.test.mjs`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add api/_vs-sectors.mjs api/_vs-sectors.test.mjs
git commit -m "feat: 대결판 주도권 축 — 섹터 대표 3종목 평균·순위 이동

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: 누가 축 — 기관 세부 6칸

**Files:**
- Modify: `api/_vs-flow.mjs` (파서가 버리던 `v[3]~v[8]`을 남긴다)
- Modify: `api/_vs-axes.mjs` (`flowAxis` 추가)
- Modify: `api/_vs-flow.test.mjs`, `api/_vs-axes.test.mjs`

**Interfaces:**
- Consumes: `_vs-flow.parseInvestorTimePage`가 이제 `inst: {금융투자, 보험, 투신, 은행, 기타금융, 연기금}`을 함께 낸다
- Produces:
  - `INST_KEYS = ['금융투자','연기금','투신','기타금융','보험','은행']` (화면 표시 순서)
  - `flowAxis(rowT, rowY)` → `{ main:{개인,외국인,기관}, inst:[{key,t,y,turned}] }`
    - 각 `main` 값은 `{t,y,turned}` — `turned`는 부호가 바뀌었는지(`true`/`false`/`null`)
    - `rowT`/`rowY`가 없으면 `null` 반환

**주의:** 시간대별 표는 `시간 | 개인 | 외국인 | 기관계 | 금융투자 | 보험 | 투신 | 은행 | 기타금융 | 연기금 | 기타법인` 순이다(11칸). 머리글에 `기관`이 colspan 그룹으로 하나 더 있어 **머리글 수와 값 칸 수가 다르다** — 인덱스로 읽되 합계 0 검사로 매번 확인한다(§6 #4).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// api/_vs-flow.test.mjs 에 추가
import { parseInvestorTimePage } from './_vs-flow.mjs';

test('기관 세부 6칸을 함께 낸다 — 합은 기관계와 같다', () => {
  // 실사고 없는 합성 행: 개인 -9641 / 외국인 -13443 / 기관계 10502 / 세부 6 / 기타법인 12582
  const tds = ['13:30', '-9641', '-13443', '10502', '4226', '189', '1798', '-64', '130', '4224', '12582'];
  const html = '<tr>' + tds.map((v) => `<td>${v}</td>`).join('') + '</tr>';
  const rows = parseInvestorTimePage(html);
  assert.equal(rows.length, 1);
  assert.equal(rows[0].기관, 10502);
  assert.equal(rows[0].inst.금융투자, 4226);
  assert.equal(rows[0].inst.연기금, 4224);
  const sum = Object.values(rows[0].inst).reduce((s, n) => s + n, 0);
  assert.ok(Math.abs(sum - rows[0].기관) <= 2, `세부 합 ${sum} vs 기관계 ${rows[0].기관}`);
});
```

```js
// api/_vs-axes.test.mjs 에 추가
import { flowAxis, INST_KEYS } from './_vs-axes.mjs';

const row = (개인, 외국인, 기관, inst) => ({ t: '13:30', 개인, 외국인, 기관, inst });

test('부호가 바뀐 주체는 turned', () => {
  const T = row(-9641, -13443, 10502, { 금융투자: 4226, 연기금: 4224, 투신: 1798, 기타금융: 130, 보험: 189, 은행: -64 });
  const Y = row(5538, -12029, -5890, { 금융투자: -4336, 연기금: -47, 투신: -1848, 기타금융: 145, 보험: 75, 은행: 122 });
  const r = flowAxis(T, Y);
  assert.equal(r.main.개인.turned, true);     // +5538 → −9641
  assert.equal(r.main.외국인.turned, false);  // 둘 다 음수
  assert.equal(r.main.기관.turned, true);
  assert.deepEqual(r.inst.map((x) => x.key), INST_KEYS);
  assert.equal(r.inst[0].t, 4226);
  assert.equal(r.inst[0].y, -4336);
});

test('어제 행이 없으면 null', () => {
  assert.equal(flowAxis(row(1, 2, 3, {}), null), null);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test api/_vs-flow.test.mjs api/_vs-axes.test.mjs`
Expected: FAIL — `rows[0].inst`가 `undefined`, `flowAxis is not a function`

- [ ] **Step 3: 구현**

`api/_vs-flow.mjs`의 `parseInvestorTimePage`에서 `rows.push(...)`를 아래로 바꾼다.

```js
    // 열 순서: 개인 · 외국인 · 기관계 · [금융투자 · 보험 · 투신 · 은행 · 기타금융 · 연기금] · 기타법인
    // 머리글엔 '기관' colspan이 하나 더 있어 머리글 수 ≠ 값 칸 수다. 합계 0 검사로 매번 확인한다(§6 #4).
    rows.push({
      t: cells[0], 개인: v[0], 외국인: v[1], 기관: v[2],
      inst: { 금융투자: v[3], 보험: v[4], 투신: v[5], 은행: v[6], 기타금융: v[7], 연기금: v[8] },
    });
```

`api/_vs-axes.mjs`에 더한다.

```js
// 화면 표시 순서 — 금액이 큰 주체부터
export const INST_KEYS = ['금융투자', '연기금', '투신', '기타금융', '보험', '은행'];

const turned = (t, y) => (typeof t !== 'number' || typeof y !== 'number' ? null : Math.sign(t) !== Math.sign(y));

export function flowAxis(rowT, rowY) {
  if (!rowT || !rowY) return null;
  const one = (k) => ({ t: rowT[k], y: rowY[k], turned: turned(rowT[k], rowY[k]) });
  return {
    main: { 개인: one('개인'), 외국인: one('외국인'), 기관: one('기관') },
    inst: INST_KEYS.map((key) => ({
      key, t: rowT.inst?.[key] ?? null, y: rowY.inst?.[key] ?? null,
      turned: turned(rowT.inst?.[key], rowY.inst?.[key]),
    })),
  };
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test api/_vs-flow.test.mjs api/_vs-axes.test.mjs`
Expected: PASS — 기존 `_vs-flow` 테스트도 모두 통과해야 한다(합계 0 검사는 그대로)

- [ ] **Step 5: 커밋**

```bash
git add api/_vs-flow.mjs api/_vs-flow.test.mjs api/_vs-axes.mjs api/_vs-axes.test.mjs
git commit -m "feat: 대결판 누가 축 — 기관 세부 6칸(이미 받아오던 v[3]~v[8])

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: 장중 응답에 axes 붙이기

**Files:**
- Modify: `api/_vs-intraday.mjs`
- Modify: `api/_vs-intraday.test.mjs`

**Interfaces:**
- Consumes: Task 1~3의 `heatAxis`, `flowAxis`, Task 2의 `SECTOR_REPS`·`sectorRows`, 기존 `minuteBars`·`prevClose`
- Produces: `buildIntradayVs` 응답에 `axes` 추가
  ```
  axes: {
    heat:   { vol:{t,y,diff,judge}, amp:{t,y,diff,judge} },      // 코스피
    market: { kosdaq:{t,y,diff,judge}, kospi200:{t,y,diff,judge} },
    sectors: [ {key,label,names,t,y,diff,rank,prevRank,move,n} ],
    flow:   { main:{...}, inst:[...] } | null
  }
  ```
  조회 실패한 하위 키는 `null`. **`axes` 자체는 항상 존재한다**(클라이언트가 옵셔널 체이닝 없이 읽게).

**주의 — 조회량:** 섹터 24종목 × 2일 = 48건이 더해진다. 어제 값은 장중에 바뀌지 않으므로 **기존 `once()` 메모(§49)에 반드시 태운다.** 오늘 봉만 매 조립마다 새로 받는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// api/_vs-intraday.test.mjs 에 추가
test('axes가 항상 있고 섹터·강도·수급이 채워진다', async () => {
  const res = await buildIntradayVs({
    now: Date.parse('2026-09-16T04:30:00Z'),   // 13:30 KST
    fetchJson: fakeJson, fetchText: fakeText,
  });
  assert.equal(res.status, 'ok');
  assert.ok(res.axes, 'axes 없음');
  assert.equal(typeof res.axes.heat.vol.t, 'number');
  assert.ok(Array.isArray(res.axes.sectors));
  assert.ok(res.axes.sectors.length >= 1);
  assert.equal(res.axes.sectors[0].rank, 1);
});

test('섹터 조회가 모두 실패해도 나머지 축은 살아 있다', async () => {
  const res = await buildIntradayVs({
    now: Date.parse('2026-09-16T04:30:00Z'),
    fetchJson: failSectorsJson, fetchText: fakeText,
  });
  assert.equal(res.status, 'ok');
  assert.deepEqual(res.axes.sectors, []);
  assert.ok(res.axes.heat);
});
```

> 기존 테스트의 `fakeJson`/`fakeText` 헬퍼를 그대로 쓴다. 섹터 종목 코드 요청에도 봉을 돌려주도록 헬퍼를 확장하고, `failSectorsJson`은 `SECTOR_REPS`의 코드 요청에만 `throw`한다.

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test api/_vs-intraday.test.mjs`
Expected: FAIL — `res.axes`가 `undefined`

- [ ] **Step 3: 구현**

`_vs-intraday.mjs` 상단 import에 더한다.

```js
import { heatAxis, flowAxis } from './_vs-axes.mjs';
import { SECTOR_REPS, sectorRows } from './_vs-sectors.mjs';
```

`leadersRaw`를 만드는 `Promise.all` 뒤, `return` 앞에 넣는다.

```js
  // ── 축 확장 ── 어제 값은 장중에 안 바뀌므로 전부 once()에 태운다(§49 메모 재사용)
  const idxAxis = async (code) => {
    const [bT, bY, pT, pY] = await Promise.all([
      settle(minuteBars('index', code, T, fetchJson, '0900', hhmm), []),
      settle(once(`iY:${code}`, () => minuteBars('index', code, Y, fetchJson), hasRows), []),
      settle(once(`iBT:${code}`, () => prevClose('index', code, T, fetchJson), isNum), null),
      settle(once(`iBY:${code}`, () => prevClose('index', code, Y, fetchJson), isNum), null),
    ]);
    const tb = atOrBefore(completed(bT, hhmm), at), yb = atOrBefore(bY, at);
    const t = tb ? pct(tb.v, pT) : null, y = yb ? pct(yb.v, pY) : null;
    return { t, y, diff: t != null && y != null ? round2(t - y) : null,
             judge: t != null && y != null ? judge(round2(t - y), TH.pctPoint) : null };
  };

  const sectorCodes = SECTOR_REPS.flatMap((s) => s.codes);
  const [kosdaq, kospi200, sectorPairs] = await Promise.all([
    idxAxis('KOSDAQ'), idxAxis('KPI200'),
    Promise.all(sectorCodes.map(async (code) => {
      const [bT, bY, pT, pY] = await Promise.all([
        settle(minuteBars('item', code, T, fetchJson, '0900', hhmm), []),
        settle(once(`sY:${code}`, () => minuteBars('item', code, Y, fetchJson), hasRows), []),
        settle(once(`sPT:${code}`, () => prevClose('item', code, T, fetchJson), isNum), null),
        settle(once(`sPY:${code}`, () => prevClose('item', code, Y, fetchJson), isNum), null),
      ]);
      const tb = atOrBefore(completed(bT, hhmm), at), yb = tb ? atOrBefore(bY, tb.t) : null;
      return [code, { t: tb ? pct(tb.v, pT) : null, y: yb ? pct(yb.v, pY) : null }];
    })),
  ]);

  const axes = {
    heat: heatAxis(kT, kY, baseT, baseY, at),
    market: { kosdaq, kospi200 },
    sectors: sectorRows(Object.fromEntries(sectorPairs)),
    flow: flow ? flowAxis(flow.t, flow.y) : null,
  };
```

`return` 객체에 `axes,`를 더한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test api/_vs-intraday.test.mjs`
Expected: PASS

- [ ] **Step 5: 조회량을 실제로 센다**

```bash
node -e "
import('./api/_vs-intraday.mjs').then(async (m) => {
  let n = 0;
  const count = (fn) => (...a) => { n++; return fn(...a); };
  // 실제 네트워크로 한 번 조립해 호출 수를 센다
  const { getJson, getEucKr } = await import('./api/_vs-intraday.mjs');
  const r = await m.buildIntradayVs({ fetchJson: count(getJson), fetchText: count(getEucKr) });
  console.log('status', r.status, '| 조회', n, '건 | 섹터', r.axes?.sectors?.length);
});"
```
Expected: 장중이 아니면 `status: closed`가 나온다. 장중에 다시 돌려 **조회 수를 기록으로 남긴다**(§9 열린 질문 1).

- [ ] **Step 6: 커밋**

```bash
git add api/_vs-intraday.mjs api/_vs-intraday.test.mjs
git commit -m "feat: 장중 대결판 응답에 axes 추가 — 강도·코스닥/코스피200·섹터·기관 세부

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: 마감 후 슬롯 (`vs=close`)

**Files:**
- Create: `api/_vs-close.mjs`
- Create: `api/_vs-close.test.mjs`
- Modify: `api/intraday.mjs`

**Interfaces:**
- Consumes: `_vs-prices.minuteBars`·`prevClose`, `_vs-flow.flowAt`, Task 1~3 축 함수
- Produces:
  - `regularFlowRow(rows)` → 15:31~15:40 사이 **마지막** 행, 없으면 `null`
  - `buildCloseVs({now, fetchJson, fetchText})` → `{status:'ok'|'early'|'closed', ...}` — 장중 응답과 **같은 모양**에 `at:'1530'` 고정
    - `status:'early'` = 15:30~15:40 (정규장은 끝났는데 확정 수급 행이 아직 없다)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// api/_vs-close.test.mjs
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { regularFlowRow, buildCloseVs } from './_vs-close.mjs';

const r = (t) => ({ t, 개인: 0, 외국인: 0, 기관: 0, inst: {} });

test('15:31~15:40 사이 마지막 행을 고른다 — 애프터장 행은 쓰지 않는다', () => {
  const rows = [r('17:10'), r('16:05'), r('15:40'), r('15:35'), r('15:30')];
  assert.equal(regularFlowRow(rows).t, '15:40');
});

test('15:31 이전 행뿐이면 null — 장이 아직 안 끝난 것이다', () => {
  assert.equal(regularFlowRow([r('15:30'), r('15:20')]), null);
});

test('15:30~15:40 사이엔 early — 마감 카드를 만들지 않는다', async () => {
  const res = await buildCloseVs({ now: Date.parse('2026-09-16T06:35:00Z') }); // 15:35 KST
  assert.equal(res.status, 'early');
});

test('09:00~15:30 장중엔 closed — 장중 대결판이 담당한다', async () => {
  const res = await buildCloseVs({ now: Date.parse('2026-09-16T04:30:00Z') }); // 13:30 KST
  assert.equal(res.status, 'closed');
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test api/_vs-close.test.mjs`
Expected: FAIL — `Cannot find module './_vs-close.mjs'`

- [ ] **Step 3: 구현**

```js
// api/_vs-close.mjs
// 마감 후 대결판 — 정규장 확정(09:00~15:30) 1분봉과 15:31~15:40 수급 행으로만 만든다.
// 16:00부터 애프터장이 열려 실시간·일봉·토스 종가는 그 값을 따라간다(§48·§51).
import { isKospiHoliday, labelFromYmd } from './_market-calendar.mjs';
import { prevTradingDay, relLabel, atOrBefore, pct, round2, judge, TH, verdict } from './_vs-core.mjs';
import { minuteBars, prevClose, curve } from './_vs-prices.mjs';
import { parseInvestorTimePage } from './_vs-flow.mjs';
import { heatAxis, flowAxis } from './_vs-axes.mjs';
import { SECTOR_REPS, sectorRows } from './_vs-sectors.mjs';

export const CLOSE_AT = '1530';

// 15:31~15:40 사이 마지막 행. 16:00 이후 행은 애프터장이 섞였다.
export function regularFlowRow(rows) {
  let hit = null;
  for (const r of rows || []) {
    const t = String(r.t).replace(':', '');
    if (t >= '1531' && t <= '1540' && (!hit || t > String(hit.t).replace(':', ''))) hit = r;
  }
  return hit;
}

const FLOW_URL = (ymd, p) =>
  `https://finance.naver.com/sise/investorDealTrendTime.naver?bizdate=${ymd}&sosok=01&page=${p}`;

// 15:31~15:40 행은 최신 시각부터 오는 표의 앞쪽이 아니라 중간에 있다 — 페이지를 끝까지 훑는다.
// 과거 날짜도 같은 방식으로 조회된다(1페이지만 보면 빈손으로 보인다).
async function regularFlowOf(ymd, fetchText) {
  const first = await fetchText(FLOW_URL(ymd, 1));
  const pages = Math.max(...[...String(first).matchAll(/page=(\d+)/g)].map((m) => +m[1]).concat(1));
  let rows = parseInvestorTimePage(first);
  for (let p = 2; p <= pages && !regularFlowRow(rows); p++) {
    rows = rows.concat(parseInvestorTimePage(await fetchText(FLOW_URL(ymd, p))));
  }
  return regularFlowRow(rows);
}

const settle = (pr, fb) => pr.then((v) => v, () => fb);
const isNum = (n) => typeof n === 'number';

export async function buildCloseVs({ now = Date.now(), fetchJson, fetchText }) {
  const k = new Date(now + 9 * 3600 * 1000);
  const dash = k.toISOString().slice(0, 10);
  const hhmm = k.toISOString().slice(11, 16).replace(':', '');
  if (isKospiHoliday(k)) return { status: 'closed' };
  if (hhmm < '1530') return { status: 'closed' };   // 장중은 vs=intraday가 담당
  if (hhmm < '1540') return { status: 'early' };    // 확정 수급 행이 아직 없다

  const yDash = prevTradingDay(dash);
  const T = dash.replace(/-/g, ''), Y = yDash.replace(/-/g, '');
  const at = CLOSE_AT;

  // 한 종목/지수의 오늘·어제 정규장 봉과 기준가. 기준가는 직전 거래일 15:30 1분봉이다(§48).
  const load = async (kind, code) => {
    const [bT, bY, pT, pY] = await Promise.all([
      settle(minuteBars(kind, code, T, fetchJson, '0900', at), []),
      settle(minuteBars(kind, code, Y, fetchJson, '0900', at), []),
      settle(prevClose(kind, code, T, fetchJson), null),
      settle(prevClose(kind, code, Y, fetchJson), null),
    ]);
    return { bT, bY, pT: isNum(pT) ? pT : null, pY: isNum(pY) ? pY : null };
  };
  const rate = (o) => {
    const tb = atOrBefore(o.bT, at), yb = atOrBefore(o.bY, at);
    const t = tb ? pct(tb.v, o.pT) : null, y = yb ? pct(yb.v, o.pY) : null;
    const d = t != null && y != null ? round2(t - y) : null;
    return { t, y, diff: d, judge: d == null ? null : judge(d, TH.pctPoint) };
  };

  const sectorCodes = SECTOR_REPS.flatMap((s) => s.codes);
  const [ks, kq, k2, sectorLoaded, fT] = await Promise.all([
    load('index', 'KOSPI'), load('index', 'KOSDAQ'), load('index', 'KPI200'),
    Promise.all(sectorCodes.map(async (c) => [c, rate(await load('item', c))])),
    settle(regularFlowOf(T, fetchText), null),
  ]);
  if (!ks.bT.length) return { status: 'waiting' };
  const fY = fT ? await settle(regularFlowOf(Y, fetchText), null) : null;

  const kospi = rate(ks);
  kospi.curveT = curve(ks.bT, ks.pT, at);
  kospi.curveY = curve(ks.bY, ks.pY, at);

  const byCode = Object.fromEntries(sectorLoaded.map(([c, r]) => [c, { t: r.t, y: r.y }]));
  const foreignDiff = fT && fY ? fT.외국인 - fY.외국인 : null;

  return {
    status: 'ok',
    time: '15:30',
    today: { date: dash, label: labelFromYmd(dash) },
    prev: { date: yDash, label: labelFromYmd(yDash), rel: relLabel(yDash, dash) },
    verdict: verdict({ yLabel: relLabel(yDash, dash), kospiDiff: kospi.diff, foreignDiff }),
    kospi,
    flow: fT && fY ? { t: fT, y: fY, time: fT.t, foreignDiff, judge: judge(foreignDiff, TH.eok) } : null,
    axes: {
      heat: heatAxis(ks.bT, ks.bY, ks.pT, ks.pY, at),
      market: { kosdaq: rate(kq), kospi200: rate(k2) },
      sectors: sectorRows(byCode),
      flow: flowAxis(fT, fY),
    },
  };
}
```

`labelFromYmd`를 `_market-calendar.mjs` import에 더한다(위 import 줄에 이미 `isKospiHoliday`가 있다).

`api/intraday.mjs`의 `vs` 분기에 더한다(라우트는 늘지 않는다).

```js
  if (req.query.vs === 'close') {
    const out = await buildCloseVs({ fetchJson: getJson, fetchText: getEucKr });
    return res.status(200).json(out);
  }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test api/_vs-close.test.mjs api/_route-budget.test.mjs`
Expected: PASS — 라우트 수가 그대로여야 한다

- [ ] **Step 5: 커밋**

```bash
git add api/_vs-close.mjs api/_vs-close.test.mjs api/intraday.mjs
git commit -m "feat: 마감 후 대결판 — 정규장 확정 봉 + 15:31~15:40 수급 행

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: 카드 렌더 — 강도·주도권·누가

**Files:**
- Modify: `web/assets/vs-yesterday.js`
- Modify: `web/assets/vs-yesterday.css`
- Modify: `web/assets/vs-yesterday.test.mjs`

**Interfaces:**
- Consumes: Task 4·5의 `axes`
- Produces: `window.__vsIntraday`에 `heatCard(axes)`, `leadCard(axes)`, `flowCard(axes)`, `miniBars(prev, now, opts)` 추가
  - `miniBars(prev, now, {zero})` → 0축 세로 막대 2칸 HTML. `zero:false`면 방향 없는 양(거래량·진폭)이라 **중립 먹색**

**디자인 근거:** 시안 [2026-09-16-vs-home-design.html](../../prototypes/2026-09-16-vs-home-design.html). 기존 `.vs-card`·`.vs-stats`·`.vs-stat`·`.vs-pill`을 쓰고 `vsx-`만 더한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// web/assets/vs-yesterday.test.mjs 에 추가
test('거래량 막대는 상승/하락 색을 쓰지 않는다 — 방향이 없는 양이다', () => {
  const html = api.miniBars(190662, 142592, { zero: false });
  assert.ok(html.includes('mag'), '중립 클래스 없음');
  assert.ok(!/class="[^"]*\b(up|dn)\b/.test(html), '방향 색이 섞임: ' + html);
});

test('수급 짝 막대는 오늘과 어제 값을 함께 적는다', () => {
  const html = api.flowCard({ flow: { main: { 개인: { t: -9641, y: 5538, turned: true },
    외국인: { t: -13443, y: -12029, turned: false }, 기관: { t: 10502, y: -5890, turned: true } },
    inst: [{ key: '금융투자', t: 4226, y: -4336, turned: true }] } });
  assert.ok(html.includes('4,226'), '오늘 값 없음');
  assert.ok(html.includes('4,336'), '어제 값 없음 — 부호 반전이 안 읽힌다');
});

test('섹터 표는 "섹터 평균"이 아니라 "대표 3종목 평균"이라 적는다', () => {
  const html = api.leadCard({ market: { kosdaq: {}, kospi200: {} },
    sectors: [{ key: 'semicon', label: '반도체', names: ['삼성전자', 'SK하이닉스', '한미반도체'],
      t: 2.56, y: -0.57, diff: 3.13, rank: 1, prevRank: 3, move: 2, n: 3 }] });
  assert.ok(html.includes('대표 3종목'), '평균 정의가 안 드러남');
  assert.ok(html.includes('삼성전자'), '대표 종목 이름이 없음');
});

test('축 데이터가 없으면 카드를 그리지 않는다', () => {
  assert.equal(api.heatCard(null), '');
  assert.equal(api.flowCard({ flow: null }), '');
  assert.equal(api.leadCard({ sectors: [] }), '');
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test web/assets/vs-yesterday.test.mjs`
Expected: FAIL — `api.miniBars is not a function`

- [ ] **Step 3: 구현**

`vs-yesterday.js`에 함수를 더하고 `window.__vsIntraday`에 노출한다. 렌더 순서는 **결론(곡선) → 강도 → 주도권 → 수급**.

```js
  // 0축 세로 막대 2칸 — 어제 옅게 · 오늘 진하게. zero:false는 방향 없는 양(거래량·진폭)이라 중립색.
  function miniBars(prev, now, opts) {
    var zero = !opts || opts.zero !== false;
    if (typeof prev !== 'number' || typeof now !== 'number') return '';
    var M = Math.max(Math.abs(prev), Math.abs(now)) || 1;
    function bar(v, isNow) {
      var h = Math.max(Math.abs(v) / M * (zero ? 18 : 36), 2);
      var st = zero ? (v >= 0 ? 'bottom:50%;height:' : 'top:50%;height:') + h + 'px'
                    : 'bottom:0;height:' + h + 'px';
      var c = zero ? (v >= 0 ? 'up' : 'dn') : 'mag';
      return '<span class="vsx-bar' + (isNow ? '' : ' prev') + '"><i class="' + c + '" style="' + st + '"></i></span>';
    }
    return '<div class="vsx-mini"><div class="vsx-bars ' + (zero ? 'zero' : 'base') + '">'
      + bar(prev, false) + bar(now, true) + '</div>'
      + '<div class="vsx-ax"><span>어제</span><span class="t">오늘</span></div></div>';
  }
```

`heatCard`·`leadCard`·`flowCard`는 시안의 마크업을 그대로 옮긴다(시안 파일의 같은 이름 함수 참조).

`vs-yesterday.css` 끝에 더한다 — **기존 `vs-` 규칙은 수정하지 않는다.**

```css
/* ── 축 확장(2026-09-16) — 기존 vs- 클래스는 건드리지 않고 vsx-만 더한다 ── */
.vs-card .vsx-sec{margin-top:24px;padding-top:16px;border-top:1px solid #F1F5F9}
.vsx-why{list-style:none;margin:11px 0 0;padding:0}
.vsx-why li{display:grid;grid-template-columns:52px minmax(0,1fr);gap:10px;padding:9px 0;
  border-top:1px solid #F1F5F9;font-size:13px;line-height:1.6;color:var(--muted)}
.vsx-why li:first-child{border-top:0}
.vsx-why .k{font-size:11px;font-weight:700;color:#0F172A}
.vsx-mini{margin-top:9px}
.vsx-bars{height:40px;display:grid;grid-template-columns:repeat(2,26px);gap:8px;
  justify-content:center;position:relative}
.vsx-bars.zero::before,.vsx-bars.base::before{content:'';position:absolute;left:0;right:0;height:1px;background:var(--hair)}
.vsx-bars.zero::before{top:50%} .vsx-bars.base::before{bottom:0}
.vsx-bar{position:relative}
.vsx-bar i{position:absolute;left:0;right:0;border-radius:3px;display:block}
.vsx-bar i.up{background:var(--up)} .vsx-bar i.dn{background:var(--dn)}
.vsx-bar i.mag{background:#0F172A;opacity:.6}
.vsx-bar.prev i{opacity:.32}
.vsx-ax{display:grid;grid-template-columns:repeat(2,26px);gap:8px;justify-content:center;
  font-size:9px;color:var(--muted);text-align:center;margin-top:4px}
.vsx-ax .t{color:#0F172A;font-weight:700}
.vsx-pairs{display:grid;grid-template-columns:repeat(6,1fr);gap:10px;margin-top:4px}
.vsx-pbars{height:76px;display:grid;grid-template-columns:repeat(2,20px);gap:5px;
  justify-content:center;position:relative}
.vsx-pbars::before{content:'';position:absolute;left:0;right:0;top:50%;height:1px;background:var(--hair)}
.vsx-pv{font-size:10px;font-weight:800;text-align:center;margin-top:5px;font-variant-numeric:tabular-nums}
.vsx-pp{font-size:9px;color:var(--muted);text-align:center;margin-top:1px;font-variant-numeric:tabular-nums}
.vsx-pl{font-size:10px;color:var(--muted);text-align:center;margin-top:4px}
.vsx-tbl{width:100%;border-collapse:collapse;margin-top:8px}
.vsx-tbl th{padding:7px 8px;background:#FAFBFC;font-size:11px;font-weight:800;color:var(--muted);
  text-align:right;border-bottom:1px solid #EEF2F6;white-space:nowrap}
.vsx-tbl td{padding:8px;border-bottom:1px solid #EEF2F6;text-align:right;white-space:nowrap;
  font-size:12.5px;color:#0F172A;font-variant-numeric:tabular-nums}
.vsx-tbl tr:last-child td{border-bottom:none}
.vsx-tbl th:first-child,.vsx-tbl td:first-child{text-align:left}
.vsx-tbl td.nm{font-weight:700;white-space:normal}
.vsx-tbl td.nm small{display:block;font-size:10px;font-weight:400;color:#94A3B8;margin-top:2px}
.vsx-rk{display:inline-flex;align-items:center;justify-content:center;width:17px;height:17px;
  border-radius:5px;background:#F1F5F9;font-size:10px;font-weight:800;color:var(--muted);
  margin-right:7px;vertical-align:1px}
.vsx-rk.top{background:#0F172A;color:#fff}
@media (max-width:760px){.vsx-pairs{grid-template-columns:repeat(3,1fr);row-gap:14px}}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test web/assets/vs-yesterday.test.mjs`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add web/assets/vs-yesterday.js web/assets/vs-yesterday.css web/assets/vs-yesterday.test.mjs
git commit -m "feat: 대결판 축 카드 렌더 — 강도·주도권·수급

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: 시간대별 표시 규칙

**Files:**
- Modify: `web/assets/vs-yesterday.js`
- Modify: `web/assets/vs-yesterday.test.mjs`

**Interfaces:**
- Produces: `slotOf(date)` → `'pre'|'open'|'close'|'night'|'weekend'`, `cardsFor(slot)` → `['hero','heat','lead','flow']`의 부분집합

**규칙(설계 §3.2, 사용자 확정):**

| 슬롯 | 시각(KST 평일) | 카드 |
| --- | --- | --- |
| `pre` | 07:30~09:00 | **없음** |
| `open` | 09:01~15:30 | hero · heat · lead · flow |
| `close` | 15:40~17:00 | hero · heat · lead · flow |
| `night` | 17:00~다음 날 07:30 | **heat 1장** |
| `weekend` | 토·일 | (후속 — 지금은 없음) |

`night`에 heat만 두는 이유: **거래량은 하루 총량이라 그날의 성격을 요약해준다**(사용자 판단). 결론·주도권·수급은 `close`에서 이미 본 값이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```js
// web/assets/vs-yesterday.test.mjs 에 추가
const kst = (s) => new Date(s + '+09:00');

test('시간대 경계', () => {
  assert.equal(api.slotOf(kst('2026-09-16T07:29')), 'night');
  assert.equal(api.slotOf(kst('2026-09-16T07:30')), 'pre');
  assert.equal(api.slotOf(kst('2026-09-16T08:59')), 'pre');
  assert.equal(api.slotOf(kst('2026-09-16T09:30')), 'open');
  assert.equal(api.slotOf(kst('2026-09-16T15:30')), 'open');
  assert.equal(api.slotOf(kst('2026-09-16T15:35')), 'close');   // early — 카드는 안 그린다
  assert.equal(api.slotOf(kst('2026-09-16T17:00')), 'night');
  assert.equal(api.slotOf(kst('2026-09-19T12:00')), 'weekend'); // 토요일
});

test('장 전엔 카드가 없고 밤엔 강도만', () => {
  assert.deepEqual(api.cardsFor('pre'), []);
  assert.deepEqual(api.cardsFor('open'), ['hero', 'heat', 'lead', 'flow']);
  assert.deepEqual(api.cardsFor('close'), ['hero', 'heat', 'lead', 'flow']);
  assert.deepEqual(api.cardsFor('night'), ['heat']);
  assert.deepEqual(api.cardsFor('weekend'), []);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test web/assets/vs-yesterday.test.mjs`
Expected: FAIL — `api.slotOf is not a function`

- [ ] **Step 3: 구현**

```js
  // 시간대 슬롯 — 경계는 설계 §3.2. 15:30~15:40은 close에 들지만 API가 early를 주므로 카드가 안 그려진다.
  function slotOf(d) {
    var k = new Date(d.getTime() + (d.getTimezoneOffset() + 540) * 60000);
    var day = k.getDay(), hm = k.getHours() * 100 + k.getMinutes();
    if (day === 0 || day === 6) return 'weekend';
    if (hm >= 730 && hm < 900) return 'pre';
    if (hm >= 900 && hm <= 1530) return 'open';
    if (hm > 1530 && hm < 1700) return 'close';
    return 'night';
  }

  // 밤엔 강도만 — 거래량은 하루 총량이라 그날의 성격을 요약해준다. 나머지는 close에서 이미 본 값이다.
  var CARDS = { pre: [], open: ['hero', 'heat', 'lead', 'flow'],
                close: ['hero', 'heat', 'lead', 'flow'], night: ['heat'], weekend: [] };
  function cardsFor(slot) { return CARDS[slot] || []; }
```

`render(d)`가 `cardsFor(slotOf(new Date()))`로 그릴 카드를 정하게 고치고, `shouldPoll()`이 `pre`·`weekend`에서 API를 부르지 않게 한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test web/assets/vs-yesterday.test.mjs api/*.test.mjs`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add web/assets/vs-yesterday.js web/assets/vs-yesterday.test.mjs
git commit -m "feat: 대결판 시간대별 카드 규칙 — 장 전 없음·밤은 강도만

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 8: 브라우저 확인과 문서 갱신

**Files:**
- Modify: `docs/SERVICE_RULES.md`

- [ ] **Step 1: 로컬에서 띄운다**

```bash
vercel dev --listen 3010 --yes
```
> 토큰이 만료돼 있으면 `vercel login`이 먼저다. 실패하면 `python3 -m http.server 8792 --directory web`로 정적만 확인하고 API는 테스트로 대신한다.

- [ ] **Step 2: 다섯 시간대 × 데스크톱·모바일을 눈으로 확인한다**

확인 항목: 가로 스크롤 0 · 장 전 카드 0개 · 밤 강도 1장 · 거래량 막대가 중립색 · 섹터 표에 종목 이름.

- [ ] **Step 3: 조회량을 기록한다**

Task 4 Step 5의 스크립트를 장중에 돌려 조회 건수를 적는다. 섹터 48건이 메모로 얼마나 줄었는지 확인한다.

- [ ] **Step 4: SERVICE_RULES에 §54를 더한다**

설계 §10의 네 가지를 적는다 — 1분봉 7거래일 한계(9/16 기준 9/8까지) · 수급 표 과거 날짜 페이지네이션 · 지수 1분봉에 거래대금 없음 · 환율 분봉 없음. 그리고 **LIVE 바와 대결판 중복 해소 규칙**(대결판은 오늘 값을 다시 적지 않는다)을 남긴다.

- [ ] **Step 5: 커밋하고 푸시한다**

```bash
node --test api/*.test.mjs && node --test web/assets/*.test.mjs
git add docs/SERVICE_RULES.md
git commit -m "docs: SERVICE_RULES §54 — 축 확장에서 확인한 데이터 한계

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
git push
```

> **푸시 시각 주의(§43):** 브리핑 잡 시간대(07:25~07:45 · 16:25~16:50 · 21:15~21:40 KST)를 피한다. 생성물이 겹치면 3-way 병합이 성립하지 않아 그날 브리핑이 통째로 유실될 수 있다.

---

## 후속 (이 계획 범위 밖)

- **주말 주간 비교** — `data/kr_official_closes.json`에 주간 데이터가 충분히 쌓인 뒤 착수. 진폭은 제외한다.
- **LIVE 바 수급의 애프터장 오염**(§51 남은 갭) — 별도 작업.
- **임계값 재조정** — 한 달 운영 후 "비슷해요" 비율을 보고.
